import { Routes } from '@angular/router';
import { DepartementComponent } from './components/departement/departement.component';

export const routes: Routes = [{ path: '', component: DepartementComponent }];
